Guard has been installed.

The chart expects the {{ .Values.superuser.existingSecret }} Secret in the release namespace.
It must provide SUPERUSER_EMAIL and SUPERUSER_PASSWORD.

{{- if .Values.ingress.enabled }}
URL: https://{{ .Values.ingress.host }}
{{- else }}
To access it locally:
  kubectl -n {{ .Release.Namespace }} port-forward service/{{ include "guard.fullname" . }} 8090:{{ .Values.service.port }}
{{- end }}

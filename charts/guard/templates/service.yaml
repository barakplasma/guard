apiVersion: v1
kind: Service
metadata:
  name: {{ include "guard.fullname" . }}
  labels:
    {{- include "guard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "guard.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP

node_modules/
dist/
dev-dist/
.vite/
*.local
.env.local
.env.*.local

# Playwright / verification artifacts
test-results/
playwright-report/
screenshots/

.DS_Store
*.log

# Agent worktrees (transient, never committed)
.claude/worktrees/

# Mobile viewport check artifacts
mobile-screenshots/

# MegaLinter writes here when run locally with mega-linter-runner.
megalinter-reports/

# MiniZinc's WebAssembly assets, copied out of node_modules by
# scripts/copyMinizincAssets.mjs at prebuild/predev. Build output of a
# dependency (19 MB of .wasm), not source.
public/solver/
